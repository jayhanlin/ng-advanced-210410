import { TwidValidatorDirective } from './twid-validator.directive';

describe('TwidValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TwidValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
